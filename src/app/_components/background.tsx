'use client'
import Lightning from '@/blocks/Backgrounds/Lightning/Lightning'

export default function Background() {
  return (
    <Lightning
      hue={250}
      xOffset={0}
      speed={1.2}
      intensity={0.8}
      size={0.4}
    />
  )
}
